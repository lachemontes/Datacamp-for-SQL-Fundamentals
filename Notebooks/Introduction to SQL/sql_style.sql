-- Rewrite this query
select person_id, role from roles limit 10

-- To align the sample SQL code with standard practices, 
-- you can ensure consistency in case sensitivity and formatting
SELECT person_id, role 
FROM roles 
LIMIT 10;